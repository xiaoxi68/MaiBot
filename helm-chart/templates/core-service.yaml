apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-maibot-core
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-maibot-core
spec:
  ports:
    - name: adapter-ws
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: {{ .Release.Name }}-maibot-core
  type: ClusterIP
